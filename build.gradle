

apply plugin: 'java'
apply plugin: 'application'
mainClassName = 'calculator.app'
jar {
    archiveBaseName = 'APP'
    archiveVersion =  '0.1.0'
    manifest {
        attributes(

                'Main-Class': 'calculator.app'
        )
    }
}